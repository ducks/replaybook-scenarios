services:
  app:
    build: .
    ports:
      - "8080:8080"
    # memswap == mem so the cache can't quietly spill into swap and dodge
    # the OOM killer on hosts that have swap enabled
    mem_limit: "64m"
    memswap_limit: "64m"
