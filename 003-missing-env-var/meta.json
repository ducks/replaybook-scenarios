{
  "id": "003-missing-env-var",
  "title": "App Crashing on Boot",
  "page": "app is down. the process keeps crashing on boot. worked fine yesterday, no recent deploys.",
  "difficulty": 1,
  "tags": ["environment", "config", "app"],
  "hints": [
    "Check the app logs - the crash is loud",
    "The app loads its environment from /app/.env at startup - is everything still in there?"
  ],
  "success_condition": "http_200",
  "success_target": "http://localhost:8080/health",
  "break": [
    { "cp": { "service": "app", "src": "app-broken.env", "dest": "/app/.env" } },
    { "restart": { "service": "app" } }
  ]
}
