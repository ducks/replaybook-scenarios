services:
  redis:
    image: redis:7-alpine
    command: redis-server --requirepass correctpassword

  sidekiq:
    build: .
    environment:
      REDIS_URL: redis://:correctpassword@redis:6379/0
    depends_on:
      - redis
