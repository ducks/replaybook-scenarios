services:
  app:
    image: python:3.12-alpine
    command: python -m http.server 3000
    networks:
      - internal

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - "8080:80"
    depends_on:
      - app
    networks:
      - internal

networks:
  internal:
